using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IfMob : MonoBehaviour {
    public GameObject Mob,Wall;
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
       // if (Mob.GetComponent<MuzhickAI>().m.Hp<1) Wall.GetComponent<Collider2D>().enabled = false;
	}
}
